#include <stdio.h>
#include <stdlib.h>

int main(){
    int i, n;
    int arr[n], newArr[n];
    scanf("%i", &n);
    for (i=0;i<n;i++){
        scanf("%i", &arr[i]);
    }

            if ((i%2)=0)
            newArr[i]=arr[i];
        printf("%i", newArr[i]);
    }
 return 0;
}
