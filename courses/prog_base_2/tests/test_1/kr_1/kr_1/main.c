
/*
	    Отримати стан радіо: працює або ефірна пауза
	//  Отримати назву радіо
	//  Змінити назву радіо
	-   Отримати назву поточного треку, що відтворюється
	//  Додати у кінець списку відтворення новий трек
	//  Видалити будь-який трек
	   (Unit-Test) Отримати кількість та список треків відтворення
	   Переключити трек на наступний
	   Додати слухача у список слухачів радіо
	   Видалити слухача зі списку слухачів
	   Отримати кількість слухачів радіо
	   Подія: при переключенні на наступний трек всім слухачам радіо. У колбек передати інформацю про попередній та про наступний трек.
*/
/*


#include "radio.h"
#include "test.c"

int main(void){
	test();
	/*
	radio_t * radio1 = radio_new();
	radio_changeName(radio1, "RADIO");
	printf("%s\n", radio_getName(radio1));
	track_t t1 = radio_newTrack();
	track_t t2 = radio_newTrack();
	track_t t3 = radio_newTrack();
	track_t t4 = radio_newTrack();
	trackList_t * trackList = radio_newTrackList(5);
	radio_addTrack(trackList, t1);
	radio_addTrack(trackList, t2);
	radio_addTrack(trackList, t3);
	radio_addTrack(trackList, t4);
	radio_trackDelete(trackList, 0);
	printf("%d\n", radio_length(trackList));
	radio_trackListFree(trackList);
	radio_free(radio1);
	*/
/*
	getchar();
	return 0;
}
*/